/* Minimal hand-maintained stub of LLVM's generated abi-breaking.h.
   pydemumble builds without ABI-breaking checks (see the
   LLVM_DISABLE_ABI_BREAKING_CHECKS_ENFORCING define in CMakeLists.txt). */
#ifndef LLVM_ABI_BREAKING_CHECKS_H
#define LLVM_ABI_BREAKING_CHECKS_H

#define LLVM_ENABLE_ABI_BREAKING_CHECKS 0
#define LLVM_ENABLE_REVERSE_ITERATION 0

#endif
